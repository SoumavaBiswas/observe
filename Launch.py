import subprocess                           # Module for launching scripts

subprocess.call(["python3", "Welcome.py"])  # Launch welcome tour
subprocess.call(["python3", "first.py"])        # Launch main UI
